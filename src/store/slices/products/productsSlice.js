import { createSlice} from '@reduxjs/toolkit';

export const productsSlice = createSlice({
name: 'products',
initialState : {
    // counter: 0 
},
reducers: {
    
    },
});
export const {someAction} = productsSlice.actions;
